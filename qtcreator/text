


FROM ubuntu:24.04

ENV DEBIAN_FRONTEND=noninteractive
ENV QT_QPA_PLATFORM=xcb
ENV LANG=C.UTF-8
ENV LC_ALL=C.UTF-8

RUN apt-get update && apt-get install -y \
    qtcreator \
    build-essential \
    cmake \
    ninja-build \
    git \
    gdb \
    qt6-base-dev \
    qt6-tools-dev \
    libxcb-xinerama0 \
    libxcb-cursor0 \
    libxkbcommon-x11-0 \
    xauth \
    dbus-x11 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace

CMD ["qtcreator"]


docker build -t qtcreator-container .
  
xhost +   (Без этого не работало !!!)

docker run --rm -it \
  --name qtcreator \
  -e DISPLAY=$DISPLAY \
  -e QT_X11_NO_MITSHM=1 \
  -v /tmp/.X11-unix:/tmp/.X11-unix:rw \
  -v "$PWD":/workspace \
  -v "$HOME/.config/QtProject":/root/.config/QtProject \
  -v "$HOME/.cache":/root/.cache \
  --device /dev/dri \
  qtcreator-container

Работает !!!

